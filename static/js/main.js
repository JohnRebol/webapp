// Footer year
document.getElementById("year").textContent = new Date().getFullYear();

// TODO: visit counter hookup goes here.
// e.g. fetch('/api/visits').then(r => r.json()).then(d => {
//   document.getElementById('visit-counter').textContent = `Visit #${d.count}`;
// });

// TODO: email button hookup goes here.
// e.g. wire a form or a "Send message" button to POST /api/contact
